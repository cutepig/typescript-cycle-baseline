{
  "name": "money-funny-sonny",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "typescript": "bash -c \"tsc --outDir ts-build\"",
    "typescript:watch": "bash -c \"tsc -w --outDir ts-build\"",
    "browserify": "bash -c \"browserify ts-build/index.js -o public/index.js\"",
    "watchify": "bash -c \"watchify ts-build/index.js -o public/index.js\"",
    "assets": "bash -c \"mkdir -p public && cp -r assets/* public\"",
    "server": "http-server public",
    "build": "npm run assets && npm run typescript && npm run browserify",
    "start": "bash -c \"npm run assets & npm run typescript:watch & npm run watchify & npm run server\""
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@cycle/dom": "^10.0.0-rc9",
    "@cycle/rxjs-run": "^3.0.2",
    "browserify": "^13.0.1",
    "rxjs": "^5.0.0-beta.9",
    "typescript": "^1.8.10",
    "xstream": "^5.0.6"
  },
  "devDependencies": {
    "http-server": "^0.9.0",
    "tslint": "^3.12.1",
    "tslint-immutable": "^0.1.2",
    "typings": "^1.3.0",
    "watchify": "^3.7.0"
  }
}
